const Bottle = require('./dist/bottle.js');
const b = Bottle.pop();

b.factory('Parent.A', () => {
    let count = 0;
    return {
        getCount : () => {
            return ++count;
        }
    };
});

b.factory('Parent.B', () => {
    let count = 0;
    return {
        getCount : () => {
            return ++count;
        }
    };
});

console.log(b.container.Parent.A.getCount());
console.log(b.container.Parent.A.getCount());
console.log(b.container.Parent.A.getCount());

b.resetProviders();
console.log(b.container.Parent.A.getCount());

